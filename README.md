# Getting Started with Restaurants Finder

## How to run project

- Install dependencies
  ```console
    npm install
    npm install -g serve
  ```
- Generate build
  ```console
    npm run build
  ```
- Serve build
  ```console
    npm serve -s build
  ```
